<% 
const title = 'Register';
const { AuthUtils } = require('../../utils/auth-helpers.js');

const registerConfig = {
  title: 'Create Account',
  action: '/auth/register',
  fields: [
    { 
      name: 'name', 
      label: 'Name', 
      type: 'text', 
      autocomplete: 'name' 
    },
    { 
      name: 'email', 
      label: 'Email', 
      type: 'email', 
      autocomplete: 'email' 
    },
    { 
      name: 'password', 
      label: 'Password', 
      type: 'password', 
      autocomplete: 'new-password',
      minlength: 8,
      hint: 'Minimum 8 characters'
    }
  ],
  submitText: 'Register',
  footerLink: `Already have an account? <a href="/auth/login">Login here</a>`,
  error
};
%>
<%- include('../layout-wrapper', { body: AuthUtils.renderForm(registerConfig), user }) %>
