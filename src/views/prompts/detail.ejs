<% const title = prompt.title; %>
<%- include('../layout-wrapper', { body: `
<div class="content-container-lg">
  <div class="mb-1">
    <a href="/prompts" class="text-primary">&larr; Back to My Prompts</a>
  </div>

  <div class="flex-between-start mb-md">
    <div>
      <h2 class="mb-xs">${prompt.title}</h2>
      <p class="text-muted">
        ${prompt.frameworkType} • Created <span data-format-date="${prompt.createdAt}" data-format="date">${prompt.createdAt}</span>
      </p>
    </div>
    <div class="flex gap-0-5">
      <button onclick="copyToClipboard()" class="btn btn-secondary">Copy to Clipboard</button>
      ${subscription.isPremium ? `
        <a href="/prompts/${prompt.id}/export" class="btn">Export</a>
      ` : ''}
    </div>
  </div>

  <div class="content-highlight mt-md">
    <pre id="prompt-text" class="pre-wrap">${prompt.finalPromptText}</pre>
  </div>
</div>

<script src="/js/prompt-detail.js"></script>
`, user, subscription }) %>
