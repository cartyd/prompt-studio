<% 
const title = `Question ${stepNum + 1} of ${totalSteps}`;
const { WizardUtils } = require('../../utils/wizard-helpers.js');

const wizardConfig = {
  question,
  stepNum,
  totalSteps,
  previousAnswer
};
%>
<%- include('../layout-wrapper', { body: `
<link rel="stylesheet" href="/css/wizard.css">

${WizardUtils.renderQuestionForm(wizardConfig)}

${WizardUtils.renderScript(question, totalSteps, stepNum)}
`, user, subscription }) %>
