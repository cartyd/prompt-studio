<% 
const title = 'Your Recommended Framework';
const { WizardUtils } = require('../../utils/wizard-helpers.js');
%>
<%- include('../layout-wrapper', { body: `
<link rel="stylesheet" href="/css/wizard.css">

${WizardUtils.renderRecommendation(recommendation).replace('value=""', `value="${csrfToken}"`)}
`, user, subscription }) %>
