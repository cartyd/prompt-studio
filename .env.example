# Database
# Development: relative paths work fine
DATABASE_URL="file:./prisma/data/dev.db"
# Production: use absolute paths to avoid resolution issues
# DATABASE_URL="file:/var/www/prompt-studio/prisma/data/prod.db"

# Session
SESSION_SECRET="your-session-secret-here-change-in-production"

# Server
PORT=3000
NODE_ENV=development
APP_URL=http://localhost:3000

# Azure AD / Microsoft 365 Email Configuration
# Create an app registration in Azure Portal (https://portal.azure.com)
# Grant Mail.Send application permission and admin consent
AZURE_TENANT_ID=your-tenant-id-here
AZURE_CLIENT_ID=your-client-id-here

# Authentication Method 1: Certificate (Recommended for Production)
# Path to PEM-encoded certificate file (certificate + private key)
AZURE_CERTIFICATE_PATH=/path/to/certificate.pem

# Authentication Method 2: Client Secret (Alternative, simpler for development)
# Use either certificate OR client secret, not both
AZURE_CLIENT_SECRET=your-client-secret-here

# Email configuration
EMAIL_FROM=pfs-noreply@yourdomain.com

# For production with SQLite:
# DATABASE_URL="file:/app/prisma/data/prod.db"
# APP_URL=https://yourdomain.com
